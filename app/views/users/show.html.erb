<% provide(:title, @user.name) %>

<div class="row">
	<aside class="span4">
		<section>
			<h1>
			<%= gravatar_for @user %>
			<div class="clearfix"></div>
			<%= @user.name %>
			</h1>
		</section>
	</aside>

</div>